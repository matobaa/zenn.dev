---
title: "作業用および対象ノードを起動する"
---

### 前提条件

`apple/container` のネットワークを正しく設定するには、macOS 26 以降が必要です。以前のバージョンではコンテナ間通信ができません。

### Apple Container をインストールする

[apple/container](https://github.com/apple/container)の最新版（本記事執筆時点では1.1.0）を[ダウンロード](https://github.com/apple/container/releases/tag/1.1.0)し、インストールします。
インストール後、`apple/container`を起動します。初期状態だと、container VMはDNSドメイン`internal`に収容されます。

```shell:m4mac
setopt interactivecomments
# ↑ これを実行することで、そのzshでは#が行コメント扱いになります
# 以下、出力例を行コメント等で示します

# brew install container, or install container from pkg

container --version
# container CLI version 1.1.0 (build: release, commit: 5973b9c)

container system start
# Launching container-apiserver...
# Testing access to container-apiserver...
# Verifying machine API server is running...
# No default kernel configured.
# Install the recommended default kernel from [https://github.com/kata-containers/kata-containers/releases/download/3.28.0/kata-static-3.28.0-arm64.tar.zst]? [Y/n]: Y
# Installing kernel...

container system status
# FIELD              VALUE
# status             running
# appRoot            /Users/matobaa/Library/Application Support/com.apple.container/
# installRoot        /usr/local/
# logRoot            
# apiserver.version  container-apiserver version 1.1.0 (build: release, commit: 5973b9c)
# apiserver.commit   5973b9cc626a3e7a499bb316a958237ebe14e2ed
# apiserver.build    release
# apiserver.appName  container-apiserver

container system dns list
# DOMAIN
# internal
```

## 2. 仮想マシンを作成する

まず、systemdが動くコンテナイメージを準備します:
```shell:m4mac
cat >Dockerfile <<-EOF
FROM debian:13-slim as systemd
RUN DEBIAN_FRONTEND=noninteractive apt-get update \
    && apt-get install -y systemd vim procps \
    && rm -rf /var/lib/apt/lists/* \
    /lib/systemd/system/multi-user.target.wants/* \
    /etc/systemd/system/*.wants/* \
    /lib/systemd/system/local-fs.target.wants/* \
    /lib/systemd/system/sockets.target.wants/*udev* \
    /lib/systemd/system/sockets.target.wants/*initctl* \
    /lib/systemd/system/basic.target.wants/* \
    /lib/systemd/system/anaconda.target.wants/*
CMD ["/lib/systemd/systemd"]
EOF
container build -t systemd -f Dockerfile
```

作成したイメージを使い、作業用1台、コントロールプレーン3台、ワーカー3台を起動します。

```shell:m4mac
container delete -f buildkit
container run -d --tmpfs /run --tmpfs /tmp --name jumpbox systemd
container run -d --tmpfs /run --tmpfs /tmp --cap-add ALL --memory 2G --name control-0 systemd
container run -d --tmpfs /run --tmpfs /tmp --name control-1 systemd
container run -d --tmpfs /run --tmpfs /tmp --name control-2 systemd
container run -d --tmpfs /run --tmpfs /tmp --name worker-0 systemd
container run -d --tmpfs /run --tmpfs /tmp --name worker-1 systemd
container run -d --tmpfs /run --tmpfs /tmp --name worker-2 systemd

container list | sort -k 5
: 出力例<<_____
ID         IMAGE           OS     ARCH   STATE    ADDR          CPUS  MEMORY
jumpbox    systemd:latest  linux  arm64  running  192.168.64.3  4     1024 MB
control-0  systemd:latest  linux  arm64  running  192.168.64.4  4     1024 MB
control-1  systemd:latest  linux  arm64  running  192.168.64.5  4     1024 MB
control-2  systemd:latest  linux  arm64  running  192.168.64.6  4     1024 MB
worker-0   systemd:latest  linux  arm64  running  192.168.64.7  4     1024 MB
worker-1   systemd:latest  linux  arm64  running  192.168.64.8  4     1024 MB
worker-2   systemd:latest  linux  arm64  running  192.168.64.9  4     1024 MB
_____
ifconfig -l
# lo0 gif0 stf0 anpi0 anpi1 anpi3 en0 en5 en6 en7 en2 en3 en4 bridge0 ap1 en1 awdl0 llw0 utun0 utun1 utun2 utun3 vmenet0 bridge100 vmenet1 vmenet2 vmenet3 vmenet4 vmenet5 vmenet6
ifconfig bridge100 inet
# bridge100: flags=8a63<UP,BROADCAST,SMART,RUNNING,ALLMULTI,SIMPLEX,MULTICAST> mtu 1500
#   options=63<RXCSUM,TXCSUM,TSO4,TSO6>
#   inet 192.168.64.1 netmask 0xffffff00 broadcast 192.168.64.255
### containerインスタンスのIPアドレス範囲は 192.168.64.xx/24 になるようだ ###
### containerインスタンス1つにつきvmenetが1つ割当たっているようだ ###
```



```
container を　1.1.0 に下げた　∵ ipv4.ip_forward = 1 できなかったため
container run -d --cap-add ALL --memory 2G --name control-0 systemd
--cap-add ALL ∵
--memory 2G ∵

cri-o
https://github.com/cri-o/packaging/blob/main/README.md#usage

apt-get install -y gpg curl
∵ software-properties-common　だとエラー


  434  kubeadm reset
  116  kubeadm init --pod-network-cidr=10.244.0.0/16
  ∵ Calicoと整合させるため

Calico
https://docs.tigera.io/calico/latest/getting-started/kubernetes/quickstart


kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.32.1/manifests/v1_crd_projectcalico_org.yaml
kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.32.1/manifests/tigera-operator.yaml
curl -O https://raw.githubusercontent.com/projectcalico/calico/v3.32.1/manifests/custom-resources.yaml
sed -Ei "/cidr/s#:.+#: 10.244.0.0/16#" custom-resources.yaml 
∵ kubeadm init --pod-network-cidr に合わせる

kubeadm join 192.168.64.3:6443 --token f0r4vf......

全ノードで
mount --make-shared /sys
mount --make-shared /
∵ Calicoが
